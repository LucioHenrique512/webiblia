import React from "react";
import { render } from "@testing-library/react";
import data from "./mooks/books.json";
import { Books } from "./containers";

test("renders learn react link", () => {
  /* const { getByTestId } = render(<Books data={data} />);
  const BooksComponent = getByTestId("main-bookslist");
  console.log(BooksComponent);
  expect(BooksComponent).toEqual(BooksComponent)*/
});
